{
  "fields": [
    {
      "name": "access-key",
      "displayName": "Access Key",
      "description": "Tenable VM access key to connect to the API",
      "placeholder": "Your Tenable VM Access Key",
      "isRequired": true,
      "isSecret": true,
      "stringField": {
        "rules": {
          "isRequired": true
        }
      }
    },
    {
      "name": "base-url",
      "displayName": "Base URL",
      "description": "Tenable Vulnerability Management API base URL. Defaults to the commercial cloud; use https://fedcloud.tenable.com for FedRAMP Moderate.",
      "stringField": {
        "defaultValue": "https://cloud.tenable.com"
      }
    },
    {
      "name": "enable-on-provision",
      "displayName": "Enable user on provisioning",
      "description": "Enable user on provision if disabled",
      "boolField": {}
    },
    {
      "name": "log-level",
      "description": "The log level: debug, info, warn, error",
      "isOps": true,
      "stringField": {
        "defaultValue": "info"
      }
    },
    {
      "name": "log-level-debug-expires-at",
      "description": "The timestamp indicating when debug-level logging should expire",
      "isOps": true,
      "stringField": {}
    },
    {
      "name": "otel-collector-endpoint",
      "description": "The endpoint of the OpenTelemetry collector to send observability data to (used for both tracing and logging if specific endpoints are not provided)",
      "isOps": true,
      "stringField": {}
    },
    {
      "name": "otel-collector-endpoint-tls-cert",
      "description": "A PEM-encoded certificate to use as a CA for TLS connections to the OpenTelemetry collector",
      "isOps": true,
      "stringField": {}
    },
    {
      "name": "otel-collector-endpoint-tls-cert-path",
      "description": "Path to a file containing a PEM-encoded certificate to use as a CA for TLS connections to the OpenTelemetry collector",
      "isOps": true,
      "stringField": {}
    },
    {
      "name": "otel-collector-endpoint-tls-insecure",
      "description": "Allow insecure connections to the OpenTelemetry collector",
      "isOps": true,
      "boolField": {}
    },
    {
      "name": "otel-logging-disabled",
      "description": "Disable OpenTelemetry logging",
      "isOps": true,
      "boolField": {}
    },
    {
      "name": "otel-tracing-disabled",
      "description": "Disable OpenTelemetry tracing",
      "isOps": true,
      "boolField": {}
    },
    {
      "name": "secret-key",
      "displayName": "Secret Key",
      "description": "Tenable VM secret key to connect to the API",
      "placeholder": "Your Tenable VM Secret Key",
      "isRequired": true,
      "isSecret": true,
      "stringField": {
        "rules": {
          "isRequired": true
        }
      }
    }
  ],
  "displayName": "Tenable VM",
  "helpUrl": "/docs/baton/tenable-vm",
  "iconUrl": "/static/app-icons/tenable-vm.svg"
}